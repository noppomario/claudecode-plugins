{
  "description": "noppomario's mods. Today: mermaid fences drawn as box-drawing text on the terminal. Needs CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1; builds without function hooks ignore the modules key.",
  "modules": [
    "./register.ts"
  ]
}
