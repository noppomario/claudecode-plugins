node_modules/

# Claude Code type declarations are Anthropic's copyrighted material.
# Fetch them locally with scripts/fetch-types.sh; never commit them.
vendor/
.claude/types/

# The probe writes its journal here.
.probe.log

# Extracted from the local Claude Code binary by scripts/extract-renderer.mjs.
plugins/mermaid/hooks/renderer.mjs
